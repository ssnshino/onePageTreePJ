# PLAN INDEX

Current development family: `v0.x`

## Default
Read `head/` first.

## History
Older plans are retained under `history/`.
Do not scan history during normal startup. Read it only for regressions, prior decisions, removed approaches, or version comparisons.
