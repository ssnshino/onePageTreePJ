# SAMPLE ARCHIVE

Early one-page Three.js experiments are preserved here as immutable historical samples.

- `v0.1/` basic shader water
- `v0.2/` real reflection
- `v0.3/` clear lake
- `v0.4/` lake shore
- `v0.5/` trees & rocks

The active baseline is also copied at repository root for fast startup.
