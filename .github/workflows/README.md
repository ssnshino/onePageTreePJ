# GitHub Actions
Workflow definitions will live here when CI is introduced.

Planned candidates:
- static HTML / JavaScript checks
- browser smoke test
- artifact preview
- optional deployment after Human-approved main merge
